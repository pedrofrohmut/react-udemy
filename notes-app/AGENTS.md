# AGENTS.md

## Workspace

Working directory: `notes-app/`

## Tech Stack

- React 19 + Vite
- Tailwind CSS 4 (uses `@tailwindcss/vite` plugin)

## Commands

- `yarn dev` - Start dev server
- `yarn build` - Production build
- `yarn lint` - ESLint
- `yarn preview` - Preview production build

## Notes

- No tests configured
- No TypeScript in current apps
- The sibling `rating-ui/` app is unrelated; do not reference it